basePath: /
definitions:
  models.SwaggerGame:
    properties:
      ano_lancamento:
        type: integer
      created_at:
        format: date-time
        type: string
      deleted_at:
        format: date-time
        type: string
      descricao:
        type: string
      desenvolvedor:
        type: string
      genero:
        type: string
      id:
        type: integer
      imagem:
        type: string
      nome:
        type: string
      nota:
        type: integer
      updated_at:
        format: date-time
        type: string
    type: object
host: localhost:8080
info:
  contact: {}
  description: This is a sample server for a favorite games API.
  title: Favorite Games API
  version: "1.0"
paths:
  /games:
    get:
      description: Get a list of all games
      parameters:
      - description: Sort field
        in: query
        name: sort
        type: string
      - description: Sort order (asc or desc)
        in: query
        name: order
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            items:
              $ref: '#/definitions/models.SwaggerGame'
            type: array
        "500":
          description: Internal Server Error
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Get all games
      tags:
      - games
swagger: "2.0"
